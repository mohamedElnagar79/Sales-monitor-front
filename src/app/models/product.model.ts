export interface Product {
  name: string;
  price: number;
  soldPrice: number;
  stock: number;
  min_stock: number;
  createdAt: string;
  updatedAt: string;
  id: number;
  description: string;
}
